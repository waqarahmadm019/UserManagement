package com.usersmanagement;


import java.util.ArrayList;

import org.springframework.context.support.ClassPathXmlApplicationContext;
 
//import com.usersmanagement.dao.DaoImplementation;
//import com.usersmanagement.model.TableModel;
 
/**
 * Standalone application with Spring Data JPA, Hibernate and Maven
 * 
 * @author DevCrumb.com
 */
public class App {
    public static void main(String[] args) {
//        ClassPathXmlApplicationContext context = new ClassPathXmlApplicationContext(
//                "applicationContext.xml");
//        DaoImplementation dao = (DaoImplementation) context.getBean("DaoImplementation");
//        ArrayList<TableModel> records = new ArrayList<TableModel>();//(List<TableModel>) dao.findAll(1, 75);
//        records.addAll(dao.findAll(1, 75));
//        for (TableModel model : records) {
//            System.out.println(model.getStarttime());
//        }
// 
//        context.close();
    }
}
